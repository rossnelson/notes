<!doctype html>
%html
  %head
    %meta{:charset => "utf-8"}/
    / Always force latest IE rendering engine or request Chrome Frame
    %meta{:content => "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
    / Use title if it's in the page YAML frontmatter
    %title= current_page.data.title || "Notes by Simian Creative"
    = stylesheet_link_tag "normalize", "app"
    = javascript_include_tag  "//ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js", "//underscorejs.org/underscore-min.js", "//backbonejs.org/backbone-min.js", "//cdnjs.cloudflare.com/ajax/libs/backbone-localstorage.js/1.1.0/backbone.localStorage-min.js", "all"
  %body{:class => page_classes}
    = yield
    .document-footer
      .designer
        .title
          • The minimal notepad •
      .developer
        .sub-title
          developed by
        .logo
          = image_tag 'logo.png'

      .driven
        .left
          %p
            driven by
        .right
          %p
            = link_to "middleman", '//middlemanapp.com/'
            \: makes developing websites simple
          %p
            and
          %p
            = link_to 'backbone', '//backbonejs.org/'
            \: gives structure to web applications
